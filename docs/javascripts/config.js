// EDIT THIS FILE: paste your Supabase project URL and anon key below.
// You can find these in your Supabase project's Settings -> API page.
// Both values are designed to be public -- they're safe to commit to a public repo.
//
// While these still contain the YOUR_PROJECT_REF / YOUR_ANON_KEY_HERE placeholders,
// the progress-tracking JS will silently no-op and the site behaves exactly as it did
// before sign-in was added. Anonymous browsing is never affected.
window.SUPABASE_URL = "https://eugemphtgzsrosjqcirk.supabase.co";
window.SUPABASE_ANON_KEY = "sb_publishable_FFgpmwhivoTjeHb8yG8ZlA_pFi4Yavj";

// Note: the Google Client ID does NOT go here. It goes in your Supabase
// dashboard under Authentication -> Providers -> Google. Supabase handles
// the Google handshake; the browser only ever sees the Supabase keys above.
